export declare function hexToPixel(q: number, r: number, size: number): {
    x: number;
    y: number;
};
export declare function getHexCorners(x: number, y: number, size: number): {
    x: number;
    y: number;
}[];
